const nome = 'Giuliano García';




/**
 * Iterando string com for of
 * ============================
 * ele retorna o valor de cada indice.
 * ele só funciona com iteraveis(que tem indice, como array ou strings).
 * ele não funciona com objetos.
 */

for ( let valor of nome){
    console.log(valor);
}